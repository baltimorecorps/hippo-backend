get:
  tags:
    - "Contact Profile"
  summary: "Get a contact's full profile"
  description: "Get all data to generate a user's profile"
  operationId: "getContactFullProfile"
  produces:
    - "application/json"
  parameters:
  responses:
    "200":
      description: "successful operation"
      schema:
        $ref: "../models/contact.yaml#Contact_Full"
    "400":
      description: "Invalid status value"
  security:
    - petstore_auth:
        - "write:pets"
        - "read:pets"
