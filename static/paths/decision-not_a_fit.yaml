post:
  tags:
    - "Staff and Employer Decisions"
  summary: "Staff or Employer marks an application to be not a fit"
  description: "Staff or Employer marks an application to be not a fit"
  operationId: "decisionNotAFit"
  produces:
    - "application/json"
  responses:
    "201":
      description: "successful operation"
      schema:
        $ref: "../swagger.yaml#definitions/Application_Full"
    "404":
      description: ""
  security:
    - petstore_auth:
        - "write:pets"
        - "read:pets"
