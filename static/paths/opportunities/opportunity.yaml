put:
  tags:
    - "Opportunities"
  summary: "Update a new opportunity"
  description: "Update a new opportunity"
  operationId: "updateOpportunity"
  produces:
    - "application/json"
  parameters:
    - in: body
      name: contact's program apps
      description: Contact info and program apps.
      schema:
        $ref: "../../models/payload.yaml#Opportunity_Payload"
  responses:
    "200":
      description: "successful operation"
      schema:
        $ref: "../../swagger.yaml#definitions/Opportunity"
    "400":
      description: "Invalid status value"
  security:
    - petstore_auth:
        - "write:pets"
        - "read:pets"
