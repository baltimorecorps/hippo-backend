post:
  tags:
    - "Opportunities"
  summary: "Activate an opportunity"
  description: "Activate an opportunity"
  operationId: "activateOpportunity"
  produces:
    - "application/json"
  parameters:
  responses:
    "200":
      description: "successful operation"
      schema:
        $ref: "../../swagger.yaml#definitions/Opportunity"
    "400":
      description: "Invalid status value"
  security:
    - petstore_auth:
        - "write:pets"
        - "read:pets"
