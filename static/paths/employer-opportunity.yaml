get:
  tags:
    - "Opportunities"
  summary: "Get an opportunity with its applications"
  description: "Get an opportunity with its applications"
  operationId: "getEmployerOpportunity"
  produces:
    - "application/json"
  parameters:
  responses:
    "200":
      description: "successful operation"
      schema:
        $ref: "../swagger.yaml#definitions/OpportunityApps"
    "400":
      description: "Invalid status value"
  security:
    - petstore_auth:
        - "write:pets"
        - "read:pets"
