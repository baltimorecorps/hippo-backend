get:
  tags:
    - "Capabilities and Skills"
  summary: "Get all capabilities"
  description: "Get an experience"
  operationId: "getAnExperience"
  produces:
    - "application/json"
  parameters:
  responses:
    "200":
      description: "successful operation"
      schema:
        type: "array"
        items:
          $ref: "../models/capabilities.yaml#Capabilities"
    "400":
      description: "Invalid status value"
  security:
    - petstore_auth:
        - "write:pets"
        - "read:pets"
